const testArr = [8, 7, 2, 5, 3, 1];

const testSum= 10;

const sumInArray = (arr, sum) => {
  console.log('arr.sort(a<b) (╯°□°)╯︵ ┻━┻ ', arr.sort().reverse())
}

console.log('sumInArray(testArr, testSum) (╯°□°)╯︵ ┻━┻ ', sumInArray(testArr, testSum))